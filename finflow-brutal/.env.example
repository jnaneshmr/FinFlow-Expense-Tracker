# FinFlow Frontend Environment Variables
# Copy this to .env and fill in your values

# API base URL — when using Vite proxy, leave as /api
VITE_API_URL=/api

# If not using Vite proxy (e.g. for production), point directly to backend:
# VITE_API_URL=https://your-api-domain.com/api
